const logStatus = status =>
    `******************** Test finished with status: ${status} ************************************`;
const logTitle = title => `*************** ${title} ***************`;
const LOG_START =
    '********************************************** START TEST **************************************************************';

export { logStatus, logTitle, LOG_START };
